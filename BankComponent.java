// Interface that all of the bank components (users, accounts, etc.) implement 
public interface BankComponent {

    ID getID(); // all bank components must have a getID method as they all have a unique ID 

}
